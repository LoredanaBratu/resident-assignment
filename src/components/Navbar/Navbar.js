import React from "react";
import StyledNavbar from "./StyledNavbar";

const Navbar = () => {
  return (
    <StyledNavbar>
      <h2 className="navbar">User projects</h2>
    </StyledNavbar>
  );
};

export default Navbar;
